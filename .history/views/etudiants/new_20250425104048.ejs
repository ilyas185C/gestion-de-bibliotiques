<!DOCTYPE html>
<html lang="fr">
<head>
  <%- include('../partials/head') %>
  <title>Ajouter un Étudiant</title>
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="container mt-5">
    <h1>Ajouter un Nouvel Étudiant</h1>
    
    <% if (typeof erreur !== 'undefined') { %>
      <div class="alert alert-danger" role="alert">
        <%= erreur %>
      </div>
    <% } %>
    
    <form action="/etudiants" method="POST">
      <div class="form-group">
        <label for="matricule">Matricule:</label>
        <input type="text" class="form-control" id="matricule" name="matricule" required>
      </div>
      
      <div class="form-group">
        <label for="nom">Nom:</label>
        <input type="text" class="form-control" id="nom" name="nom" required>
      </div>
      
      <div class="form-group">
        <label for="prenom">Prénom:</label>
        <input type="text" class="form-control" id="prenom" name="prenom" required>
      </div>
      
      <div class="form-group">
        <label for="email">Email:</label>
        <input type="email" class="form-control" id="email" name="email" required>
      </div>
      
      <div class="form-group">
        <label for="telephone">Téléphone:</label>
        <input type="text" class="form-control" id="telephone" name="telephone">
      </div>
      
      <button type="submit" class="btn btn-primary">Ajouter</button>
      <a href="/etudiants" class="btn btn-secondary">Annuler</a>
    </form>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>

// views/etudiants/show.ejs
<!DOCTYPE html>
<html lang="fr">
<head>
  <%- include('../partials/head') %>
  <title>Détails de l'Étudiant</title>
</head>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="container mt-5">
    <h1>Détails de l'Étudiant</h1>
    
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><%= etudiant ? etudiant.prenom : '' %> <%= etudiant ? etudiant.nom : '' %></h5>
        <p class="card-text"><strong>Matricule:</strong> <%= etudiant ? etudiant.matricule : '' %></p>
        <p class="card-text"><strong>Email:</strong> <%= etudiant ? etudiant.email : '' %></p>
        <p class="card-text"><strong>Téléphone:</strong> <%= etudiant && etudiant.telephone ? etudiant.telephone : 'Non renseigné' %></p>
        
        <a href="/etudiants/<%= etudiant._id %>/edit" class="btn btn-warning">Modifier</a>
        <form action="/etudiants/<%= etudiant._id %>?_method=DELETE" method="POST" class="d-inline">
          <button class="btn btn-danger" onclick="return confirm('Êtes-vous sûr de vouloir supprimer cet étudiant?')">Supprimer</button>
        </form>
        <a href="/etudiants" class="btn btn-secondary">Retour à la liste</a>
      </div>
    </div>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>